#include <iostream>
using namespace std;

int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int N, U, D;
        cin >> N >> U >> D;
        int H[N];
        for (int i = 0; i < N; i++)
        {
            cin >> H[i];
        }
        
    }
    return 0;
}
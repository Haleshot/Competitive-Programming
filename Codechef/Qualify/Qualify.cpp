#include <iostream>
using namespace std;

int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int X, A, B;
        cin >> X >> A >> B;
        if (A + (B * 2) >= X)
        {
            cout << "QUALIFY" << endl;
        }
        else
        {
            
        }
    }
    return 0;

}
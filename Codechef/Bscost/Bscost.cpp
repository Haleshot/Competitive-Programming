#include <iostream>
using namespace std;


int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int N, X, Y;
        cin >> N >> X >> Y;
        string S, a, b;
        cin >> S;
        int L[N];
        if (X > Y)
        {

        }
        for (int i = 0; i < N; i ++)
        {
            a = S[i];
            b = S[i + 1];
            if (a == "0")
            {

            }

        }
    }

    return 0;
}
#include <iostream>
using namespace std;
#define MAX 100

int main()
{
    int T = 0, N, K = 0;
    cin >> T;
    while(T--)
    {
        cin >> N >> K;
        int L[MAX];

        for(int i = 0; i <= N; i++)
        {
            
        }


    }
    return 0;
}
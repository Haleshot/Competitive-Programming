#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int N;
        cin >> N;
        int A[N];
        for (int i = 0; i , N; i++)
        {
            cin >> A[i];
        }
        int e = 1, y = 0;
        sort(A, A + N, greater<int>());

    }
    return 0;
}
T = int(input())
while T:
    T -= 1
    actities = map(str, input.split())
    contest_won = map(str, input.split())
    top_contrib = input()
    bug_found = map(str, input.split())
    contest_host = input()
    
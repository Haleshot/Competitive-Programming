#include <iostream>
using namespace std;


int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int N, M, X, Y;
        cin >> N >> M >> X >> Y;
        int inp[N];
        for (int i = 0; i < N; i++)
        {
            cin >> inp[i];
        }
    }
    return 0;
}